struct Punto ini
    [2] entero: z;
    entero : x;
    entero: y;
fin

struct A ini
    Punto : p;
    real : r;
fin

princ
ini
    entero : tamA = size(A);
    entero : tamPunto = size(Punto);
    entero : tamEntero = size(entero);
    Punto: p1;
    Punto: p2;
    p1.x = 10;
    p1.y = 20;
    imprimir(p1.x);
    imprimir(p1.y);
    imprimir(p1.x + p1.y);
fin